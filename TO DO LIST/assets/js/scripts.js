var btnSubmit = document.getElementById("btnSubmit").onclick = function() {
}

var text = document.getElementById("text")

function Adicionar Tarefa() {
    currentNumber = currentNumber + 1;
    currentNumberWrapper.innerHTML = currentNumber;
}

function decrement() {
    currentNumber = currentNumber - 1;
    currentNumberWrapper.innerHTML = currentNumber;
}